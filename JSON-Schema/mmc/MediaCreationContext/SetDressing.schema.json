{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "MediaCreationContext#SetDressing",
	"title": "SetDressing",
	"description": "Objects used on set to create and enhance the environment. Set dressing differs from a prop because the characters’ interaction with it may not be explicitly mentioned in, or required by, the narrative.",
	"type": "object",
	"properties": {
		"Identifier": {
			"title": "Identifier",
			"type": "array",
			"items": {
				"$ref": "../Utility/Identifier.schema.json#"
			}
		},
		"Name": {
			"$ref": "../Utility/Name.schema.json#"
		},
		"description": {
			"title": "Description",
			"type" : "string",
			"description": "A brief description of the set dressing"
		}
	}
}